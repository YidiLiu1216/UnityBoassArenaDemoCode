using UnityEngine;

public class FSMBuilder
{
    public static StateMachine<T> BuildFSM<T>(T context)
    {
        //add the states and transitions to the FSM
        // Set the initial state of the FSM
        var fsm = new StateMachine<T>();
        //set initial state to the first state in the list
        return fsm;
    }
}
